App\Entity\Cart:
  cart_{1..100}:
    selections (unique): '<numberbetween(1, 4)>x @selection_*'
    total: '<numberbetween(100, 3000)>'
    status: "<randomElement(['commande en preparation', 'commande prete', 'commande emportee'])>"

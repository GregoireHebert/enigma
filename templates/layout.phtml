<html>
    <head>
        <title>Mon super site</title>
    </head>
    <body>
        <h1>Mon super site !</h1>
        <p>
            <a href="{{ path(accueil) }}">Home</a>
            <a href="{{ path(info) }}">Info</a>
            <a href="{{ path(query, {"name": "greg"}) }}">Query</a>
        </p>
        {{ content|raw }}
    </body>
</html>

{% extends 'base.html.twig' %}

{% block title %}Modifier {{ album.name }}{% endblock %}

{% block body %}
    <main class="bg-white mx-auto p-8 md:p-12 my-10 rounded-lg shadow-2xl">
        <section>
            <h3 class="font-bold text-2xl">Modifier {{ album.name }}</h3>
        </section>

        <section class="mt-10">
            {{ include('album/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            {{ include('album/_delete_form.html.twig') }}
        </section>
    </main>

{% endblock %}

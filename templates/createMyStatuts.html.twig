{% extends 'base.html.twig' %}

{% block title %}Créer un nouveau Statut{% endblock %}

{% block body %}

    {#    {{ dump(app.session) }}#}

    {{ render(controller('App\\Controller\\ListStatuts')) }}

    {% if is_granted('ROLE_MANAGER') or app.user.getUsername() == 'greg' %}
        Création d'un nouveau Statut:

        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    {% endif %}

{% endblock %}